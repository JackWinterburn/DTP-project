# Supabase client (Phase 2)

EU-region Postgres project. Anonymous clients may insert sessions/results and
select a result only by its share_token; email_optins is insert-only. See
Architecture doc Section 4 for the schema and RLS policy.
