# Supabase (EU region project) -- Phase 2
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# AI parent-card generation (server-side only, never exposed to client) -- Phase 6
GEMINI_API_KEY=

# Email opt-in (optional) -- Phase 6
RESEND_API_KEY=

# Public site origin -- used to build absolute QR-code URLs (Risk R4: move this to
# an ada.ac.uk subdomain once available, not *.vercel.app) -- ticket #20
NEXT_PUBLIC_SITE_URL=
